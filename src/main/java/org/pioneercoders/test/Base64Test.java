package org.pioneercoders.test;

import org.pioneercoders.utils.AppConfig;
import org.springframework.beans.factory.annotation.Autowired;


// Java Base64 Encoder / Java Base64 Decoder Example

public class Base64Test {

	
	public static void main(String[] args) {
	/*	try {
			String originalText = "nekkala.appaji@gmail.com";
			String encodedBytes = new BASE64Encoder().encodeBuffer(originalText.getBytes());
			char[] encoderCharArray = encodedBytes.toCharArray();
			for (int i = 0; i < encoderCharArray.length; i++) {
				int ascii = (int) encoderCharArray[i];
				encoderCharArray[i] = (char) (ascii + 1);
			}

			System.out.println("This is the data after encoding"
					+ encoderCharArray.toString());

			String cypherText = new String(encoderCharArray);

			char[] arrayData = cypherText.toCharArray();
			for (int j = 0; j < arrayData.length; j++) {
				int ascii = (int) arrayData[j];
				arrayData[j] = (char) (ascii - 1);
			}

			byte[] decodedBytes = new BASE64Decoder().decodeBuffer(new String(arrayData));
			System.out.println("decodedBytes :" + new String(decodedBytes));

		} catch (IOException e) {
			e.printStackTrace();
		}*/
		System.out.println("hai"+"\n"+"helloow");
		
	}
	
	
}